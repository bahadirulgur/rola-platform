services:
  rola-api:
    build:
      context: ./src/Rola.Gateway.Api
      dockerfile: Dockerfile
    container_name: rola-api
    ports:
      - "3001:3000"
    environment:
      ASPNETCORE_URLS: "http://+:3000"
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
    restart: unless-stopped