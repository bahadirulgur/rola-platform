services:
  rola-api:
    build:
      context: ./src/Rora.Gateway.Api
      dockerfile: Dockerfile
    container_name: rola-api
    ports:
      - "3000:3000"
    environment:
      ASPNETCORE_URLS: "http://+:3000"
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
    restart: unless-stopped